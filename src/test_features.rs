fn main() {
    // This is just to trigger a compile error that lists available features if possible,
    // or I'll use introspection if possible.
    // Actually, I'll try to use a macro to print them if I could, but Rust doesn't have that for enums/structs easily.
    // I'll just try common variations.
}
